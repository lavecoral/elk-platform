server:
  port: 11001
spring:
  application:
    name: demo-service-b
management:
  info:
    git:
      mode: full
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
